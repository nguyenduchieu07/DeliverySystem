<header class="topbar">
    <label for="navToggle" class="btn" title="Menu">☰</label>
    <div class="brand">
        <div class="logo"></div>
        <div class="name">Store Admin</div>
    </div>
    <div style="display:flex">
        <div class="search">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M21 21l-4.2-4.2" stroke="#99a3b3" stroke-width="2" stroke-linecap="round" /></svg>
            <input placeholder="Tìm đơn, khách hàng, dịch vụ..." />
        </div>
        <div class="actions">
            <button type="button" class="btn" title="Thông báo">🔔</button>
            @* <button type="button" class="btn primary" title="Tạo báo giá">+ Báo giá</button> *@
            <a class="btn primary" href="@Url.Action("Index", "Quotations",new {tab = "draft"})"><span class="text">+ Báo giá</span></a>
        </div>
    </div>
</header>